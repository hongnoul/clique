# client/ — user-facing frontends

Python SDK (`sdk.py`), headless CLI for monitor-less nodes like the GX10
(`cli.py`), textual TUI (`tui.py`), and web dashboard specs (`dashboard/`).
See ../SPEC.md.
