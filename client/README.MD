# client/ — user-facing frontends

Python SDK (`sdk.py`), headless CLI (`cli.py`), button home (`home.py`),
textual TUI (`tui.py`), stdlib-only curl TUI (`curl_tui.py`), MCP server
(`mcp_server.py`, `mcp_install.py`), background launcher (`daemon.py`),
and web dashboard specs (`dashboard/`). See ../CODE_EDIT_PLAN.md.
